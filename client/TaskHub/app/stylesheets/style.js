import { StyleSheet } from "react-native";

const lightPurple = "#9D8FC5";
const darkPurple = "#6759A7";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  flexbox: {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
  },
});
